* Myenv: 個人の環境設定に必要なファイル集

ここは、個人の計算機(PC等)環境設定に必要・有用な
ファイルを集めたものである。
ただし、基本的に設定ファイルを対象とするものとする。
ツール類は自作であれば専用リポジトリを作成すべし。

* ファイルと簡易説明
  詳細な説明は各ファイルの冒頭に記載するものとし、
  ここでは各ファイルの内容を一行程度で概説する。
  できるだけカテゴリごとに分類し、リストを更新する。


** dotfiles
   - bash_profile.self: .bash_profileインポート(主に環境変数)
   - bashrc.self: .bashrcからインポート(上記以外)
   - bashrc.special: 汎用的ではないが有用な小物設定格納庫
   - gitconfig: gitコマンドのglobal設定内容

** cfgfiles
   アプリケーションごとの設定ファイルを配置する。
   詳細は cfgfiles/README を参照のこと。

** dictool
   個人辞書の操作に関連するツール群を配置する。
   辞書そのものは内容に機微なものが含まれる可能性が
   あるものはここでは管理しないよう注意する。
   フリー配布の辞書本体などはこの限りではない。
   - anthy2gji.*: Anthyの個人辞書をGoogle日本語入力の
     辞書として使えるようにするための関連ツール群。

** その他
  

* Local Variables
# Local Variables: ;
# mode: org ;
# coding: utf-8 ;
# End: ;
